[package]
name = "codewhale-telemetry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Opt-in product telemetry client for Codewhale"

[dependencies]
anyhow.workspace = true
chrono.workspace = true
codewhale-config = { path = "../config", version = "0.9.5" }
codewhale-paths = { path = "../paths", version = "0.9.5" }
codewhale-release = { path = "../release", version = "0.9.5" }
fd-lock = "4.0.4"
reqwest = { workspace = true, features = ["blocking"] }
serde.workspace = true
serde_json.workspace = true
tempfile.workspace = true
tracing.workspace = true
uuid.workspace = true

[build-dependencies]
codewhale-build-support = { path = "../build-support", version = "0.9.5" }

[dev-dependencies]
# Used as an *assertion*, never as a filter: every string leaf of a serialized
# batch is run through `redact_for_disclosure` and must come back unredacted.
codewhale-workflow = { path = "../workflow", version = "0.9.5" }
